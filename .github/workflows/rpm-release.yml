name: build rpm on release

on:
  push:
  #release:
    #types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: build rpm 
      id: rpm
      run: |
          cd ./packaging/rpm
           docker build -t pamoauth2device-rpm-build .
           docker run -v ${{ github.workspace }}:/data pamoauth2device-rpm-build 
           docker exec 1b026874da3a bash -c 'chmod +r rpmbuild/RPMS'
           docker cp 1b026874da3a:/rpmbuild/RPMS /data
           
    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: PAM oauth2 RPMs
        path: /data
